import 'package:flutter/material.dart';

class DSColors {
  static const primary = Color.fromARGB(255, 0, 155, 198);
  static const primaryDark = Color.fromARGB(255, 2, 72, 91);
  // static const primary = Color.fromRGBO(45, 84, 116, 1);
  // static const primaryDark = Color.fromARGB(255, 25, 30, 40);
}
