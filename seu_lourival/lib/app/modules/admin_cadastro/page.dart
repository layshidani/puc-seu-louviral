import 'package:flutter/material.dart';
import 'package:get/instance_manager.dart';
import 'controller.dart';

class Admin_cadastroPage extends StatelessWidget {
  Admin_cadastroPage({Key? key}) : super(key: key);
  final _controller = Get.find<Admin_cadastroController>();
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
